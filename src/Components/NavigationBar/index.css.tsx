import { css } from "@emotion/react";
import { colors, rem } from "../../shared/css";

export default css`
  height: ${rem(64)};
  background-color: ${colors.black};
  width: 100%;
  color: ${colors.white};
`;
